{
  "tagName": "sherpa-layout-grid",
  "description": "Main page content grid. Use it inside an app shell to hold the page header, optional side rails, and the primary content area. Slotted <sherpa-container data-variant=\"resizable\"> children take grid spans from their data-col-span / data-row-span attributes.",
  "category": "shell",
  "tier": 1,
  "group": "core",
  "baseClass": "SherpaElement",
  "attributes": [
    {
      "name": "data-row-height",
      "type": "string",
      "description": "CSS length for grid-auto-rows (default: 160px)"
    },
    {
      "name": "data-content",
      "type": "enum",
      "description": "\"static\" — stacks children vertically instead of using the CSS grid"
    },
    {
      "name": "data-fill",
      "type": "enum",
      "description": "\"viewport\" — clamps the layout to the viewport height"
    },
    {
      "name": "data-pad",
      "type": "boolean",
      "description": "Adds padding inside the content surface"
    },
    {
      "name": "data-gap",
      "type": "enum",
      "description": "sm | base | lg — gap between stacked children in static mode",
      "enumValues": [
        "sm",
        "base",
        "lg"
      ]
    },
    {
      "name": "data-editable",
      "type": "boolean",
      "description": "Enables drag-to-reposition for slotted containers"
    }
  ],
  "slots": [
    {
      "name": "view-header",
      "description": "Optional sherpa-view-header above the content surface"
    },
    {
      "name": "side-panel-start",
      "description": "Leading side rail"
    },
    {
      "name": "side-panel-end",
      "description": "Trailing side rail"
    },
    {
      "name": "",
      "description": "Main page content (sherpa-container tiles, filters, tables, charts, and similar content)"
    }
  ],
  "events": [
    {
      "name": "layout-reorder",
      "description": "Fires after a successful reposition.",
      "bubbles": true,
      "composed": true,
      "detail": {
        "from": "number",
        "to": "number",
        "order": "string[] }  // order is the"
      }
    }
  ],
  "cssProperties": [
    {
      "name": "--maxColCount",
      "description": "Integer column count (default 12, registered)"
    },
    {
      "name": "--row-height",
      "description": "Row height (default 160px, registered)"
    }
  ]
}
