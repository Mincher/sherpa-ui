{
  "tagName": "sherpa-progress-tracker",
  "description": "Vertical timeline with status-coloured milestone nodes plus a header row (title + percentage). For a simpler atomic timeline without header chrome, use {@link SherpaStepper} with `data-template=\"timeline\"` directly. This component composes that primitive's visual model and adds the tracker header.",
  "category": "feedback",
  "baseClass": "SherpaElement",
  "attributes": [
    {
      "name": "data-heading",
      "type": "string",
      "description": "Tracker heading text"
    },
    {
      "name": "data-percentage",
      "type": "string",
      "description": "Completion text (e.g. \"60% Complete\")"
    }
  ],
  "slots": [],
  "events": [],
  "methods": [
    {
      "name": "setMilestones(milestones)",
      "description": "Set milestones: [{ label, status, description?, timestamp? }]",
      "params": [
        {
          "name": "milestones",
          "type": "any"
        }
      ]
    }
  ],
  "properties": [
    {
      "name": "milestones",
      "type": "Array",
      "description": "Current milestones array (getter-only)"
    }
  ]
}
