.DS_Store
node_modules/
.venv/
.vscode/
*.code-search

# Figma extract output (large; regenerate with `npm run tokens:extract`).
# alias-snapshot.json IS tracked — it is the canonical Alias source until
# the design team rebuilds the Figma Alias collection.
figma-tokens/figma-variables.json
figma-tokens/figma-variables.prev.json
figma-tokens/Apex*/
figma-tokens/Classic/

# Stale commit message draft
.git-commit-msg.tmp

# Build metrics history
.build-metrics.json

# Build cache (hash-based incremental build system)
.build-cache/
