@@ -30,7 +30,7 @@
   override async onInputRender(): Promise<void> {
     this.#toggleBtnEl = this.$<HTMLElement>('.toggle-visibility');
   }
 
   override onInputConnect(): void {
     this.#toggleBtnEl?.addEventListener('click', this.#onToggle);
   }
@@ -57,12 +57,14 @@
 
   #updateVisibility(): void {
     const el = this.getInputElement();
-    if (el) el.type = this.#visible ? 'text' : 'password';
+    if (el && 'type' in el) {
+      (el as HTMLInputElement).type = this.#visible ? 'text' : 'password';
+    }
     if (this.#toggleBtnEl) {
       this.#toggleBtnEl.setAttribute('data-icon-start',
         this.#visible ? '' : ''
       );
-      this.#toggleBtnEl.dataset.label = this.#visible ? 'Hide' : 'Show';
+      (this.#toggleBtnEl as any).dataset['label'] = this.#visible ? 'Hide' : 'Show';
     }
   }
 }
